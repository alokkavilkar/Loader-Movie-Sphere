def imageName = 'alokkavilkar/loader'
pipeline {
    agent {
        label 'worker'
    }

    stages {
        stage('Unit Test')
        {
            script {
                sh "echo ${imageName}"
            }
        }
    }
}