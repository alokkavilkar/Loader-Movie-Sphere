def imageName = 'alokkavilkar/loader'
pipeline {
    agent {
        label 'worker'
    }

    stages {
        stage('Unit Test')
        steps{
            script {
                sh "echo ${imageName}"
            }
        }
    }
}